# waybar module dependencies
# Status bar + supporting tools for widgets
packages:
  arch:
    - waybar
    - playerctl
    - swaync
  debian:
    - waybar
    - playerctl
  fedora:
    - waybar
    - playerctl
  macos: []

aur:
  - wttrbar
