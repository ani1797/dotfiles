# WayVNC Configuration Template
# This is a template file. To use it:
# 1. Run 'configure-wayvnc' to automatically set everything up, OR
# 2. Manually copy this to 'config' and customize

# Network binding
# Use 0.0.0.0 to allow connections from other machines on the local network
# Use 127.0.0.1 for localhost-only access (requires SSH tunnel for remote access)
address=0.0.0.0

# VNC port (default VNC port is 5900)
port=5900

# Authentication and encryption
# Enable authentication to prevent unauthorized access
enable_auth=true

# Use PAM authentication (system user credentials)
# This means VNC will use your Linux login username and password
enable_pam=true

# TLS certificates for encrypted connection
# These files will be generated by the configure-wayvnc script
# Use absolute paths for systemd service compatibility
# Replace /home/USERNAME with your actual home directory path
private_key_file=/home/USERNAME/.config/wayvnc/tls_key.pem
certificate_file=/home/USERNAME/.config/wayvnc/tls_cert.pem

# RSA key for RSA-AES encryption (RealVNC / Apple VNC compatibility)
# Must be in PKCS#1 format (BEGIN RSA PRIVATE KEY, not BEGIN PRIVATE KEY)
# Generate with: openssl genrsa -traditional -out rsa_key.pem 4096
rsa_private_key_file=/home/USERNAME/.config/wayvnc/rsa_key.pem

# Optional: Keyboard layout configuration
# Uncomment and customize if you need a specific layout
# xkb_layout=us
# xkb_variant=
# xkb_model=pc105

# Remote Desktop Access
# =====================
# With address=0.0.0.0, connect from any machine on the local network:
#   vnc-client HOSTNAME:5900
#
# With address=127.0.0.1, use SSH tunneling for remote access:
#   ssh -L 5900:localhost:5900 user@hyprland-host
#   Then connect your VNC client to localhost:5900
#
# Authentication: Use your system username and password
