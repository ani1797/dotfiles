#!/bin/sh
# has - Check if a command exists
# Usage: has <command> && echo "yes"
# Exit code: 0 if command exists, 1 if not

if [ $# -eq 0 ]; then
    echo "Usage: has <command>" >&2
    exit 1
fi

command -v "$1" >/dev/null 2>&1
