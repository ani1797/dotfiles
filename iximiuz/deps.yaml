provides: labctl

# Define installation script once using YAML anchor
script_install: &install_script
  run: curl -sf https://labs.iximiuz.com/cli/install.sh | sh
  provides: labctl

packages:
  arch:
    - *install_script
  debian:
    - *install_script
  fedora:
    - *install_script
  macos:
    - *install_script
