# 1Password SSH Agent (conditional)
# Only routes through 1Password when the agent socket actually exists.
Match host * exec "test -S %d/.1password/agent.sock"
  IdentityAgent ~/.1password/agent.sock
