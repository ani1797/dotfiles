[global]
require-virtualenv = true
no-cache-dir = false

[install]
require-virtualenv = true

# Machine-specific overrides:
# pip supports layered config: /etc/pip.conf -> ~/.config/pip/pip.conf -> per-venv
# For machine-specific settings, use environment variables:
#   PIP_INDEX_URL=https://pypi.corp.example.com/simple/
#   PIP_TRUSTED_HOST=pypi.corp.example.com
#   PIP_CERT=/path/to/corporate-ca-bundle.pem
#
# Or create a site-level /etc/pip.conf for system-wide corporate overrides.
# See pip.conf.local.example for patterns.
